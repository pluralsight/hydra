application.name = hydra_test

hydra_test {
  schema.registry.url = "mock"

  ingest {
    classpath-scan = ["hydra.ingest.test.TestIngestor"]
  }
}

akka {
  remote {
    artery {
      enabled = off
      canonical.hostname = "127.0.0.1"
      canonical.port = 5552
    }
  }
}