package hydra.core.ingest

// $COVERAGE-OFF
object NotificationParams {
  /**
    * Used to publish to a Slack channel other than the default channel.
    */
  val SLACK_NOTIFICATION_CHANNEL_PARAM = "slack-notification-channel"
}
