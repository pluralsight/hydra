application.name = hydra_test

hydra_test {
  schema.registry.url = "mock"
}

akka.actor.test-ext.default {
  type = Dispatcher
  executor = "fork-join-executor"
  fork-join-executor {
    parallelism-min = 2
    parallelism-factor = 2.0
    parallelism-max = 6
  }
  throughput = 100
}